// written by the touch controls, read by Player every frame
export const touch = {
  // joystick, -1 to 1. y is forward
  x: 0,
  y: 0,
  // how far the camera was dragged since the last frame (radians)
  turn: 0,
}
