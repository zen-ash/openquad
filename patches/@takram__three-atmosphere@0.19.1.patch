diff --git a/build/webgpu.js b/build/webgpu.js
index 1ce72679646fb617bb62b52dc7f62bf3aebc208a..053aa5540684b6c399533921c510d6d56d73455b 100644
--- a/build/webgpu.js
+++ b/build/webgpu.js
@@ -16,8 +16,8 @@ const H = "float", Ao = "float", q = "float", Fs = "float", Fo = "float", en = "
   "DensityProfileLayer"
 ), te = /* @__PURE__ */ ut(
   {
-    layer0: Re.layout.name,
-    layer1: Re.layout.name
+    layer0: Re.name,
+    layer1: Re.name
   },
   "DensityProfile"
 ), os = {
@@ -26,13 +26,13 @@ const H = "float", Ao = "float", q = "float", Fs = "float", Fo = "float", en = "
   sunAngularRadius: Ao,
   bottomRadius: H,
   topRadius: H,
-  rayleighDensity: te.layout.name,
+  rayleighDensity: te.name,
   rayleighScattering: At,
-  mieDensity: te.layout.name,
+  mieDensity: te.name,
   mieScattering: At,
   mieExtinction: At,
   miePhaseFunctionG: q,
-  absorptionDensity: te.layout.name,
+  absorptionDensity: te.name,
   absorptionExtinction: At,
   groundAlbedo: qt,
   minCosLight: q,
@@ -1470,18 +1470,12 @@ class Ie {
 }
 const { resetRendererState: Jo, restoreRendererState: ti } = Ot;
 async function ei(n) {
+  // openquad: run every step now. waiting for idle time between them left the lookup
+  // tables half made (and the sky black) for as long as the page stayed busy
   const t = n[Symbol.iterator]();
-  return await new Promise((e, s) => {
-    const o = () => {
-      try {
-        const { value: i, done: a } = t.next();
-        a === !0 ? e(i) : Vs(o);
-      } catch (i) {
-        s(i instanceof Error ? i : new Error());
-      }
-    };
-    Vs(o);
-  });
+  let r = t.next();
+  for (; r.done !== !0; ) r = t.next();
+  return r.value;
 }
 let Je;
 function ve(n, t) {
@@ -2110,7 +2104,7 @@ class yi extends Qt {
           )
         ), m.assign(
           a.transpose().mul(w(h, 0)).xyz
-        ), l.assign(On(m.xzy));
+        ), l.assign(On(m.xzy.toConst()));
       });
       const p = Ns(l).toConst(), y = bs(l).toConst(), g = Ns(m).toConst(), S = bs(m).toConst(), x = w(0).toVar();
       return D(c.lessThan(r), () => {
